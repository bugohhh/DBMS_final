server.port=8080

# Existing local MySQL database created/imported via TablePlus.
# Keep the password as a placeholder; use application-local.properties or env vars for real credentials.
spring.datasource.url=jdbc:mysql://localhost:3306/dbms-example?serverTimezone=Asia/Taipei&useSSL=false
spring.datasource.username=root
spring.datasource.password=YOUR_PASSWORD
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
